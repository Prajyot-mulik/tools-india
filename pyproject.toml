[project]
name = "india-tools"
version = "1.0.0"
description = "Free online calculators for Indian students and professionals"
requires-python = ">=3.8"
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
